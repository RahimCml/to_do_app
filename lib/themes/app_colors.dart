import 'package:flutter/material.dart';

class AppColor {
  static const Color mainColor = Color(0xff495579);
  static const Color textColor = Color(0xffffffff);
  static Color tileColor = mainColor.withOpacity(0.4);
}